import { createTheme } from "@rneui/themed";

export const theme = createTheme({
  lightColors: {
    primary: "#00BFFF",
    secondary: "#FAF0E6", 
  },
  darkColors: {
    primary: "#00BFFF",
    secondary: "#FAF0E6", 
  },
  mode: "light",
});
